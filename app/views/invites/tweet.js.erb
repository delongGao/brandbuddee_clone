$('#btn_invite_<%= params[:id] %>').hide().after('<%= j render("formtwitter") %>');
$("#txtDirectMessage<%= params[:id] %>").limit({
    limit: 140,
    id_result: "direct_message_counter<%= params[:id] %>",
    alertClass: "alert alert-error"
});
$("#btnCancel<%= params[:id] %>").click(function(event) {
  event.preventDefault();
  $('#frm_invite_<%= params[:id] %>').fadeOut('slow', function() {
    // Animation complete.
  });
  $('#btn_invite_<%= params[:id] %>').fadeIn('slow', function() {
    // Animation complete.
  });
});