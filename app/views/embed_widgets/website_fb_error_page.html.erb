<% content_for :embed_widget_index do %>
	style="width:420px !important;overflow-x:hidden !important;"
<% end %>
<div class="row" style="margin-left:-7px;margin-right:0;width:435px;margin-bottom:0px;">
  <div class="span6" style="margin-left:-7px;margin-right:0;width:435px;margin-bottom:0px;">
    <div class="well well-large" id="embed_widget" style="margin-right:0;width:435px;margin-bottom:0px;padding-top:3%;padding-bottom:3%;">
    	<% flash.each do |name, msg| %>
				<div class="alert alert-block alert-<%= name %> fade in" style="text-align:left;">
					<a href="#" class="close" data-dismiss="alert">&times;</a>
					<h4 class="alert-heading">Just so You Know...</h4>
					<p><%= msg.html_safe %></p>
				</div>
			<% end %>
			<div class="alert alert-block alert-error fade in" style="text-align:left;">
				<a href="#" class="close" data-dismiss="alert">&times;</a>
				<h4>Uh Oh!</h4>
				When connecting your account with Facebook, please make sure you accept ALL the permissions requested by the Facebook Application. Click the button below to try again.
			</div>
			<div class="btn-group" style="margin-bottom:2%; margin-top:-1.5%;">
				<a class="btn btn-info" style="font-weight:700;" href="/campaign/<%= @campaign.link %>/go_viral"><i class="icon-refresh"></i> Try Connecting Again</a>
				<a class="btn btn-success" style="font-weight:700;" href="/campaign/<%= @campaign.link %>/go_viral"><i class="icon-reply"></i> Go Back</a>
			</div>
			<p style="text-align:justify;font-size:1.25em;"><% unless @campaign.is_white_label? %>brandbuddee uses <% else %>We use <% end %>social networks like Facebook and Twitter to help you share campaigns and earn gifts. To do so, you must first connect your account, and accept certain permissions.</p>

			<p style="text-align:left;font-size:1.25em;">These permissions include:</p>
			<ul style="text-align:left;font-size:1.25em;"><li>Accessing information about your account</li>
			<li>Posting to your wall</li>
			<li>Managing your pages</li></ul>
			<p style="text-align:left;font-size:1.25em;">To try again, click the button above, or keep reading if you would like to know why we request each permission.</p>
			<% unless @campaign.is_white_label? %>
				<h4 style="margin-bottom:0; margin-top:-1.5%;font-size:1.2em;">Sample Permission Dialog</h4>
				<%= image_tag "fb-permission.jpg", alt: "#{@brand.name.parameterize}-#{@campaign.title.parameterize}", title: "#{@brand.name} | #{@campaign.title}", size: "392x161", style: "margin-bottom:1.5%;" %>
			<% end %>
			<dl style="text-align:justify;font-size:1.25em;">
				<dt>Account Information</dt>
				<dd>When you create an account, we like to ensure we have your email so when you win a reward, we know where to send the redeem details. We will NEVER share your email or contact information with others.</dd>
				<dt>Wall Posting</dt>
				<dd>One of the most popular ways to share a campaign is posting it to your Facebook Wall. To do so from within our site & app, we need access. We will only post when you click the post button, and you can customize the message.</dd>
				<dt>Page Management</dt>
				<dd>To add a campaign to a Facebook Page you manage, we will need access to manage your pages. We will never access information on a page, or add content to a page, unless you first choose to.</dd>
			</dl>
			<p style="text-align:justify;font-size:1.25em;">To recap, <% unless @campaign.is_white_label? %>brandbuddee <% else %>we <% end %>will never post to, or access information on, your Facebook Account without you first choosing to do so. It is important you accept ALL permissions requested, not picking and choosing. If you have any questions, please feel free to visit our site or contact us anytime!</p>
			<p style="font-size:1.25em;">Have a wonderful day!</p>
			<br>
			<% unless @campaign.is_white_label? %>
				<%= link_to image_tag("bb-logo.png", alt: "#{@brand.name.parameterize}-#{@campaign.title.parameterize}", title: "#{@brand.name} | #{@campaign.title}", size: "188x35", style: "margin-top:1%;"), "/", target: "_blank", title: "#{@brand.name} | #{@campaign.title}" %>
			<% else %>
				<%= image_tag @campaign.brand.brand_logo_url, alt: "#{@brand.name.parameterize}-#{@campaign.title.parameterize}", title: "#{@brand.name} | #{@campaign.title}", style: "margin-top:2.5%;max-width:35%;max-height:45px;" unless @campaign.brand.brand_logo.blank? %>
			<% end %>
		</div>
  </div>
</div>
